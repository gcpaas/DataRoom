# 【功能名称】设计规格

> 用途：作为 `docs/superpowers/specs/YYYY-MM-DD-feature-design.md` 的中文模板。生成正式规格时，请删除本说明，保留与当前功能相关的章节。

## 背景

说明当前问题、业务上下文、已有能力和本次改动的触发原因。

- 当前系统如何工作：
- 存在的问题或限制：
- 为什么现在需要处理：

## 已确认需求

列出已经明确的产品或技术需求，避免混入实现步骤。

- 【需求 1】
- 【需求 2】
- 【需求 3】

## 目标

说明本次设计必须达成的结果。

- 【目标 1】
- 【目标 2】
- 【目标 3】

## 非目标

说明本次不做什么，防止范围膨胀。

- 【非目标 1】
- 【非目标 2】
- 【非目标 3】

## 推荐方案

用一到三段说明整体方案、核心取舍和为什么选择该方案。

- 方案概述：
- 复用的现有机制：
- 主要收益：
- 已知代价：

## 数据模型

说明新增或修改的数据结构、实体、枚举、配置对象、JSON 多态子类型、表结构或字段语义。

### 【模型或实体名称】

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `fieldName` | `String` | 【说明】 |

语义：

- 【字段或状态语义】
- 【兼容旧数据的规则】
- 【敏感字段、脱敏或更新保留规则】

## 后端设计

说明后端服务、接口、权限、认证、工厂分发、异常处理和兼容策略。

### 接口设计

接口基础路径：`/dataRoom/【module】`

| 方法 | 路径 | 角色 | 说明 |
| --- | --- | --- | --- |
| `GET` | `/detail/{code}` | `developer` | 【说明】 |
| `POST` | `/save` | `developer` | 【说明】 |

请求字段：

- `fieldName`：【说明】

返回字段：

- `fieldName`：【说明】

接口响应继续使用统一包装：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

### 执行流程

按真实运行顺序描述核心流程。

1. 【步骤 1】
2. 【步骤 2】
3. 【步骤 3】
4. 【错误处理或回退逻辑】

### 权限与安全

- 访问角色：
- Token 或认证链路影响：
- 敏感字段处理：
- 审计、软删除或日志要求：
- Java `catch` 块必须记录异常栈；如涉及变更，交付前运行 `CatchBlockLoggingTest`。

## 前端交互

说明入口、页面、弹窗、表单、状态、空态、错误提示和保存逻辑。

### 入口

- 【入口位置】
- 【显示条件】
- 【触发行为】

### 表单或页面字段

- 【字段 1】：【控件类型、默认值、校验规则】
- 【字段 2】：【控件类型、默认值、校验规则】

### 交互规则

- 【保存规则】
- 【复制、刷新、启用/停用等行为】
- 【错误提示】

### 样式约束

- 涉及 Vue、SCSS、CSS、Element Plus 组件、页面布局或交互状态时，必须先阅读 `docs/design/DESIGN.md`。
- 使用 Element Plus 默认组件样式和 CSS 变量。
- 业务样式只控制外层布局、间距、滚动区域和自定义容器。
- 不覆盖 `.el-*` 内部样式，不使用 `:deep(.el-*)`、`::v-deep`、`/deep/`、`>>>` 或 `!important`。
- 不使用硬编码颜色、`--dr-*` 私有颜色变量或负字距。

## 兼容策略

说明旧数据、旧接口、旧页面配置、历史 JSON、默认值和迁移策略。

- 【兼容点 1】
- 【兼容点 2】
- 【是否需要迁移脚本】
- 【旧字段是否保留】

## 错误处理

列出用户可感知错误和系统内部错误处理方式。

- 【错误场景 1】：【提示或返回】
- 【错误场景 2】：【提示或返回】
- 【异常日志要求】

## 测试计划

### 后端

至少覆盖：

- 【测试点 1】
- 【测试点 2】
- 【测试点 3】

验证命令：

```bash
mvn test -pl dataRoomServer -Dtest=【TestClass1】,【TestClass2】
```

如果修改 Java `catch` 块，额外运行：

```bash
mvn -q -pl dataRoomServer -Dtest=CatchBlockLoggingTest -DforkCount=0 test
```

### 前端

至少覆盖：

- 【类型检查】
- 【样式规范检查】
- 【关键交互手工验证】

验证命令：

```bash
cd dataRoomFront
npm run type-check
```

涉及格式或样式规范时运行：

```bash
cd dataRoomFront
npm run lint
```

## 实施边界

本功能影响：

- 【后端模块或类】
- 【前端页面或组件】
- 【数据库或脚本】
- 【测试】

不改变：

- 【明确不改的模块或机制】
- 【明确不改的业务流程】

## 待确认问题

仅列出真正阻塞设计或实现的问题；已确认后移动到“已确认需求”或对应设计章节。

- 【问题 1】
- 【问题 2】
